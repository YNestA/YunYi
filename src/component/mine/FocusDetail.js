import React, {Component} from 'react';
import {View, Text, Image, StyleSheet,TouchableNativeFeedback} from 'react-native';
import {screenUtils} from "../../tools/ScreenUtils";

export class FocusDetail extends Component{
    constructor(props){
        super(props);
    }

    render(){
        return({

        });
    }
}